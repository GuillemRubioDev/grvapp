import RegisterForm from '../components/forms/RegisterForm';

const RegisterPage = () => {
    return (
        <div className="bg-background dark:bg-dark-background text-text dark:text-dark-text min-h-screen flex items-center justify-center px-4">
            <RegisterForm />
        </div>
    );
};

export default RegisterPage;
